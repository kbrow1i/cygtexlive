--- origsrc/texlive-20150506-source/texk/web2c/luatexdir/luazlib/lzlib.c	2015-03-20 03:59:58.000000000 -0400
+++ src/texlive-20150506-source/texk/web2c/luatexdir/luazlib/lzlib.c	2015-05-06 11:27:25.732670100 -0400
@@ -549,6 +549,7 @@ LUALIB_API int luaopen_zlib(lua_State *L
 
     /* ====================================================================== */
 
+#if 0
     /* make sure header and library version are consistent */
     const char* version = zlibVersion();
     if (strncmp(version, ZLIB_VERSION, 4))
@@ -556,6 +557,7 @@ LUALIB_API int luaopen_zlib(lua_State *L
         lua_pushfstring(L, "zlib library version does not match - header: %s, library: %s", ZLIB_VERSION, version);
         lua_error(L);
     }
+#endif
 
     /* create new metatable for zlib compression structures */
     luaL_newmetatable(L, ZSTREAMMETA);
